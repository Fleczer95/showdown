node_modules/
.expo/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
.DS_Store
dist/
.*.swp

# IDE
.idea/
.vscode/

# Autoresearch
autoresearch.jsonl
autoresearch.ideas.md
autoresearch.md

# Test coverage
coverage/

# Python bytecode (IAP provisioning scripts)
__pycache__/
*.pyc

# Python Virtual Environments
.venv/
venv/

# Native
ios/Pods/
ios/build/
android/build/
android/.gradle/
android/app/build/
local.properties
*.keystore
*.jks
bundletool-latest.jar

# Google Play service account key
google-play-key.json
api_key_play_store.txt

# Firebase Admin SDK key (challenge cleanup script)
firebase-admin-key.json

# Agent/CLI skill installs pulled in by `firebase init` — tooling, not app code
# (no trailing slash: .claude/skills entries are symlinks, which `dir/` won't match)
.agents/skills/firebase-*
.agents/skills/xcode-project-setup
.claude/skills/firebase-*
.claude/skills/xcode-project-setup
skills-lock.json

# Firebase / Google Services
google-services.json
GoogleService-Info.plist
# App Icon Proposals
assets/archived_icons/

# agent memory
**/.maestro/
*.maestro
.claude/handoffs/

# Agent tooling venv (store provisioning scripts)
.venv-agents/
